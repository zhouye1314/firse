# Read the Docs 配置文件
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"  # 你的 Python 版本，3.8+ 都可以

python:
  install:
    - requirements: requirements.txt

mkdocs:
  configuration: mkdocs.yml
